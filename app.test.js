/**const supertest = require("supertest");
const app = require("/Users/bhagyalakshmi/Documents/hrms/src/api/hrms/appli.js");
const req = supertest.agent(app);

/**describe("Get /", () => {
  skip.test("it should respond with hello world ", () => {
    req
      .get("/")
      .expect(200)
      .then(response => {
        expect(response.body).toEqual("Hello World!");
        expect(response.statusCode).toBe(200);
      });
  });
});*/
///Users/bhagyalakshmi/Documents/hrms/src/api/hrms/test/app.test.js
